= 第三章 所有容器的接口

即所有容器都共通的调用语法和方式

. 创建和删除，除了array要给容量，其他容器的创建的构造方式都一样，清除都是clear()
. 大小相关的，empty(), size(), max_size()都一样
. 迭代器
.. begin(),end(): 往后迭代的一组迭代器
.. cbegin(),cend() 往后迭代的一组const迭代器
.. rbegin(), rend(), 倒着迭代的一组迭代器
.. crbegin(),crend(), 倒着迭代的一组const迭代器
. 赋值，擦除方法都一样